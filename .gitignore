# Session transcripts and temp data
*.jsonl
weekly-summary-raw.json

# Memory (personal context, not config)
memory/
MEMORY.md

# OS/editor
.DS_Store
*.swp
*~
-e 
# Secrets (NEVER commit API keys, tokens, passwords)
secrets/
